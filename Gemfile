source 'https://rubygems.org'

gem 'rgl', '~>0.6'

group :dev do
  gem 'redcarpet' # render markdown in yard
  gem 'rspec' # run tests
  gem 'ruby-lsp' # debug in vscode
  gem 'solargraph' # develop in vscode
  gem 'webrick' # run yard docs server
end
